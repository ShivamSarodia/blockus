Run `python generate_moves.py` to generate a `moves.pickle` file. Then, run `blockus.py` to execute a simulation.

`manual_play.py` is unused. (I copied it over from the notebook to save its contents.)